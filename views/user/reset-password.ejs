<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Password</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>

    html {
        height: 100%;
    }
    body {
      background-color:#fdf6e4;
      height: 100%;
      display: flex;
      align-items: center;
    }
    .reset-password-container {
      max-width: 400px;
      margin: 50px auto;
      padding: 20px;
      background-color: transparent;
      border-radius: 10px;
    }
    .lock-icon {
      font-size: 50px;
      color: #6c757d;
    }
    form input {
        width: 100%;
        border: 1px solid #ccc;
        border-radius: 0.9rem;
        padding: 0.8rem;
        font-size: 1rem;
        outline: none;
        background-color: #ffffff;
    }

    form button {
        background-color: #5c4027;
        color: #ffffff;
        border: none;
        border-radius: 0.9rem;
        padding: 0.8rem;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.3s;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="reset-password-container text-center">
      <i class="bi bi-lock-fill lock-icon"></i> <!-- Lock icon -->
      <h3 class="mt-3">Reset Password</h3>
      <p>Enter your new password below.</p>
      <form method="post" action="/reset-password">
        <div class="mb-3">
          <input type="password" class="" name="password" id="newPassword" placeholder="Enter new password" required>
        </div>
        <div class="mb-3">
          <input type="password" class="" name="confirmPassword" id="confirmPassword" placeholder="Confirm new password" required>
        </div>
        <button type="submit" class=" w-100">Reset Password</button>
      </form>
    </div>
  </div>
  <!-- Bootstrap Icons -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Include the Swal library -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<% if (error && error.length > 0) { %>
  <script>
    Swal.fire({
      icon: "error",
      title: "Error",
      text: "<%= error[0] %>",
      confirmButtonText: "OK"
    });
  </script>
<% } %>

<% if (success && success.length > 0) { %>
 <script>
  Swal.fire({
    icon: "success",
    title: "Success",
    text: "<%= success[0] %>",
    confirmButtonText: "OK"
  });
 </script>
<% } %>

  
</body>
</html>
